server:
  port: 8081

spring:

  application:
    name: tw-driver

  datasource:
    url: jdbc:postgresql://localhost:5432/transportWala
    username: postgres
    password: Kshitiz@1710

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true


  redis:
    host: 127.0.0.1     # Redis server host (default: localhost)
    port: 6379

eureka:
  instance:
    prefer-ip-address: true
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:9000/eureka
